---
type: project
status: active
created: 2026-05-13
updated: 2026-05-13
tags:
  - 电网
  - 调价平台
  - 车网互动
  - 虚拟电厂
---

# 调价平台 - 虚拟电厂削峰填谷响应方案设计

## 背景

车网互动大脑会接收虚拟电厂下发的调控指令。调控指令通常用于让充电负荷参与电网侧调节，核心类型分为两类：

- **削峰**：虚拟电厂希望在高负荷时段降低充电负荷，避免峰值继续抬升。
- **填谷**：虚拟电厂希望在低负荷时段提升充电负荷，提高低谷电能消纳能力。

## 角色分工

| 角色     | 主要职责                     | 与调价平台的关系 |     |
| ------ | ------------------------ | -------- | --- |
| 虚拟电厂   | 下发削峰、填谷调控指令，指定响应目标、时段和约束 | 指令来源     |     |
| 车网互动大脑 | 接收指令、识别事件类型、统筹执行侧和运营侧响应  | 调价平台的上游  |     |
| 调价平台   | 生成调价、优惠建议，评估收益与响应能力      | 本方案主体    |     |
|        |                          |          |     |

## 方案设计

在现有智能调价外面加一层 
现有仍然负责日常经营场景下的分时服务费优化；车网互动任务来了以后，不直接进入原调价流程 ，也不改变原来的训练逻辑，而是把现有输出的价格结果当作**基准经营价格**，即在现有最优价格基础上临时怎么调。

1.新增一个车网互动任务接口，如：

```
POST /vgi/optimize-pricing
```

它接收车网互动大脑下发的任务：

2. 现有 PPO 结果作为“**经营基准价**”

调用现有的功能，得到正常经营场景下的：

```
优化后的服务费
预期订单
排队风险
预期市场份额
```

这个结果就是 **无车网互动任务时的基准策略**。

后续削峰、填谷都在这个基准价格上做叠加

3. 新增调价层，不进入原奖励函数
新增一个单独的奖励函数：

```
VGI_score =  
任务完成度
+ 虚拟电厂补偿收益
+ 原经营收益保留
- 用户流失惩罚
- 排队风险惩罚
- 毛利损失惩罚
```

普通 PPO 的 reward 不变。
车网互动任务只在新增里使用自己的评分函数。

4. 选择任务完成度最高、收益损失最小的方案  
5. 输出任务型调价策略

## 削峰任务

削峰任务的目标是降低目标时段负荷。

算法上做法是：

```
基准负荷 - 调价后预测负荷 = 预计削峰量
```

对于削峰，只生成涨价或减少优惠的候选动作：

```
peak_shaving_actions = [0.00, +0.05, +0.10, +0.15, +0.20]
```

每个动作都在原 PPO 推荐服务费基础上叠加：

```
candidate_price = base_service_fee + delta
```

然后用你现有的车辆仿真逻辑模拟用户是否还来本站、是否换站、是否延后充电。

## 填谷任务

填谷任务的目标是增加目标时段负荷。

算法上做法是：

```
调价后预测负荷 - 基准负荷 = 预计填谷量
```

对于填谷，只生成降价或优惠动作：

```
valley_filling_actions = [0.00, -0.05, -0.10, -0.15, -0.20]
```

每个动作同样在现有 PPO 推荐价基础上叠加：

```
candidate_price = base_service_fee + delta
```

然后仿真车辆是否会因为低价转移到目标时段、目标站点充电。得到**填谷能力**



车网互动应该做的是对削峰任务收益有个大致的判断，即可以计算预估每个站的可调潜力与预估收益，然后将调整站点和调整指标下发到调价平台，调价平台应该根据指标和站点，再去将收益放大，像调整